apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: education-provider-registry-public-api-temp.teacherservices.cloud
spec:
  ingressClassName: nginx
  rules:
  - host: education-provider-registry-public-api-temp.teacherservices.cloud
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: education-provider-registry-public-api-production
            port:
              number: 80
